# Shared compiler flags consumed by both native and WASM builds.
shared_no_exception_flags = ['-fno-exceptions', '-fno-rtti']

# Shared preprocessor defines
shared_defines = ['-DFASTLED_USE_PROGMEM=0']
